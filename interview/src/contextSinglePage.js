import { useContext } from "react";
// import {LoginContext} from '../useContext.js'

const SinglePost=()=>{
    // const login=useContext(LoginContext);
    // console.log(login);
    return(
        <h1>Single Post</h1>
    );
}
export default SinglePost;